name: Code Trigger (src/app only)

on:
  push:
    branches:
      - main
      - 'release/*'
    paths:
      - 'src/**'
      - 'app/**'

jobs:
  run-on-code:
    runs-on: ubuntu-latest

    steps:
      - name: Code change detected
        run: echo "✅ Running because src/ or app/ changed"
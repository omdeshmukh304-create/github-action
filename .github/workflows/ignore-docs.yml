name: Ignore Docs Changes

on:
  push:
    branches:
      - main
      - 'release/*'
    paths-ignore:
      - '*.md'
      - 'docs/**'

jobs:
  run-if-not-docs:
    runs-on: ubuntu-latest

    steps:
      - name: Non-doc change detected
        run: echo "✅ Running because it's NOT just docs"